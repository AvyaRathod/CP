#include<bits/stdc++.h>
using namespace std;

int main(){
    int t; cin >> t;
    while (t--){
        int a,b,c,d; cin >> a >>b >>c >>d ;
        int clap;
        if (a/b)==(c/d){
            clap = 0;
        }
        else{
            float x = ((b*c)/(d*a)) 
            if (ceil(n) == floor(n)){
                clap = 2;
            }
            else{
                clap = 1;
            }
        }
        cout << clap;
    }
}